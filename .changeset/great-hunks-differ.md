---
"@senzu/cli": minor
---

Add a Hunk generator that emits merge-ready TOML theme snippets for every Senzu variant, in both the modern `[themes.<id>]` format (hunk >= 0.18) and the legacy `[custom_theme]` format (hunk <= 0.17).
